# Runner 생성 로직 상세 설명

JIT Runner Manager의 Runner 생성 및 대기열 처리 로직을 설명합니다.

---

## 목차

1. [개요](#개요)
2. [처리 흐름](#처리-흐름)
3. [제한 확인 로직](#제한-확인-로직)
4. [대기열 처리 방식](#대기열-처리-방식)
5. [시나리오 예시](#시나리오-예시)
6. [FIFO 순서 보장](#fifo-순서-보장)
7. [FAQ](#faq)

---

## 개요

JIT Runner Manager는 GitHub Enterprise Server에서 workflow가 시작될 때 실시간으로 Runner를 생성합니다.

### 핵심 원칙

| 역할 | 담당 |
|------|------|
| Runner 생성 결정 | JIT Runner Manager |
| **대기열 관리** | **JIT Runner Manager (Redis)** |
| Webhook 발송 | GitHub Enterprise Server (1회만) |

### 중요한 점

> ⚠️ **GitHub은 `workflow_job.queued` webhook을 재전송하지 않습니다!**
>
> Webhook은 Job 상태가 변경될 때 **1회만** 발송됩니다.
> 따라서 JIT Runner Manager가 **자체 대기열**을 Redis에서 관리합니다.

---

## 처리 흐름

### 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          GitHub Enterprise Server                            │
│                                                                              │
│   ┌──────────────────────────────────────────────────────────────────────┐  │
│   │                         Workflow Jobs                                 │  │
│   │                                                                       │  │
│   │  • 새 workflow 시작 → queued 이벤트 발생 (1회만!)                     │  │
│   │  • Runner 없으면 → GitHub UI에서 "Waiting for runner" 표시           │  │
│   │  • 타임아웃 (기본 6시간) 후 실패 처리                                 │  │
│   └──────────────────────────────────────────────────────────────────────┘  │
│                              │                                               │
│                    workflow_job webhook (1회 발송)                           │
└──────────────────────────────┬───────────────────────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│                         JIT Runner Manager                                    │
│                                                                               │
│   ┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐       │
│   │ FastAPI Webhook │────▶│  Celery Worker  │────▶│  Kubernetes     │       │
│   │   Receiver      │     │                 │     │  (Pod 생성)     │       │
│   └─────────────────┘     └────────┬────────┘     └─────────────────┘       │
│                                    │                                         │
│                                    ▼                                         │
│                           ┌─────────────────┐                                │
│                           │     Redis       │                                │
│                           │  (상태 + 대기열) │  ◀── 자체 대기열 관리!        │
│                           │  • org:X:running│                                │
│                           │  • org:X:pending│  ◀── 대기 중인 Job 목록       │
│                           │  • global:total │                                │
│                           └─────────────────┘                                │
│                                                                               │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 이벤트별 처리

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        workflow_job.queued 이벤트                            │
│                                                                              │
│  1. Webhook 수신 → 라벨 확인 (code-linux)                                    │
│                         │                                                    │
│                         ▼                                                    │
│  2. Redis 대기열에 Job 저장 (모든 요청)                                      │
│     └── org:{name}:pending 리스트에 Job 정보 추가                            │
│                                                                              │
│  ※ 제한 확인 없이 무조건 대기열에 저장!                                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                  process_pending_queues (5초마다 실행)                       │
│                                                                              │
│  1. K8s 상태 동기화                                                          │
│     └── Pod 상태 조회 → Redis 카운터 갱신                                    │
│         (종료된 Pod 자동 감지)                                               │
│                         │                                                    │
│                         ▼                                                    │
│  2. 전체 제한 확인                                                           │
│     └── total_running >= max_total? ───Yes───▶ 처리 건너뜀                   │
│                         │                                                    │
│                        No                                                    │
│                         │                                                    │
│                         ▼                                                    │
│  3. 각 Org 대기열 순회                                                       │
│     │                                                                        │
│     ├── org_running >= org_limit? ───Yes───▶ 다음 Org로                      │
│     │         │                                                              │
│     │        No                                                              │
│     │         │                                                              │
│     │         ▼                                                              │
│     └── 대기열에서 Job 추출 → Runner 생성 태스크 호출                        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                       workflow_job.completed 이벤트                          │
│                                                                              │
│  ※ 처리하지 않음! (로깅만)                                                   │
│                                                                              │
│  Runner는 Ephemeral 모드로 동작하여 Job 완료 시 Pod가 자동 종료됩니다.       │
│  Pod 종료는 process_pending_queues에서 K8s 상태 조회로 감지됩니다.           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                          Pod 종료 감지 흐름                                  │
│                                                                              │
│  1. Ephemeral Runner가 Job 완료 후 자동 종료                                 │
│     └── Pod 상태: Running → Succeeded/Failed                                 │
│                         │                                                    │
│                         ▼                                                    │
│  2. process_pending_queues 실행 시 (5초마다)                                 │
│     └── K8s API로 Pod 상태 조회                                              │
│     └── Running/Pending Pod만 카운트                                         │
│     └── Redis 카운터 자동 갱신 (종료된 Pod 반영)                             │
│                         │                                                    │
│                         ▼                                                    │
│  3. 해당 Org에 여유 생김 → 대기열에서 다음 Job 처리                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 제한 확인 로직

### 2단계 제한 확인

Runner 생성 전에 두 가지 제한을 순차적으로 확인합니다:

```python
# 1. Organization 제한 확인
org_running = redis_client.get_org_running_count_sync(org_name)
org_limit = redis_client.get_effective_org_limit_sync(org_name)  # 커스텀 또는 기본값

if org_running >= org_limit:
    # Redis 대기열에 Job 정보 저장
    redis_client.add_pending_job_sync(
        org_name=org_name,
        job_id=job_id,
        run_id=run_id,
        job_name=job_name,
        repo_full_name=repo_full_name,
        labels=labels
    )
    return {"status": "pending", "reason": "org_limit_reached"}

# 2. 전체 제한 확인
total_running = redis_client.get_total_running_sync()

if total_running >= config.runner.max_total:
    # Redis 대기열에 Job 정보 저장
    redis_client.add_pending_job_sync(...)
    return {"status": "pending", "reason": "total_limit_reached"}

# 두 조건 모두 통과 → Runner 생성
```

### 제한 우선순위

```
                    요청 도착
                        │
                        ▼
            ┌───────────────────────┐
            │ Org 제한 (org_limit)   │  ← 먼저 확인
            │ 예: A org = 10개      │
            └───────────┬───────────┘
                        │
              org_running < org_limit?
                        │
              ┌─────────┴─────────┐
             Yes                  No
              │                    │
              ▼                    ▼
    ┌─────────────────┐    ┌─────────────────┐
    │ 전체 제한 확인   │    │ Redis 대기열    │
    │ max_total=100   │    │ 저장 (FIFO)     │
    └────────┬────────┘    └─────────────────┘
             │
    total_running < max_total?
             │
    ┌────────┴────────┐
   Yes               No
    │                 │
    ▼                 ▼
┌─────────┐   ┌─────────────────┐
│ Runner  │   │ Redis 대기열    │
│  생성   │   │ 저장 (FIFO)     │
└─────────┘   └─────────────────┘
```

### 커스텀 Org 제한

Organization별로 다른 제한을 설정할 수 있습니다:

| Organization | 커스텀 제한 | 적용 제한 |
|--------------|------------|----------|
| platform-team | 25 | 25 |
| small-project | 5 | 5 |
| regular-org | (없음) | 10 (기본값) |

```python
def get_effective_org_limit_sync(self, org_name: str) -> int:
    """Organization의 유효 제한 조회 (커스텀 또는 기본값)"""
    custom_limit = self.get_org_max_limit_sync(org_name)
    if custom_limit is not None:
        return custom_limit
    return self.config.runner.max_per_org  # 기본값 (예: 10)
```

---

## 대기열 처리 방식

### 핵심: JIT Runner Manager가 Redis에서 대기열 관리

**GitHub은 Webhook을 재전송하지 않으므로**, JIT Runner Manager가 자체 대기열을 관리합니다.

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                              Redis                                            │
│                                                                               │
│   대기열 (org:{name}:pending):                                                │
│   ┌─────────────────────────────────────────────────────────────────────┐    │
│   │                                                                      │    │
│   │  org:A:pending = [                                                   │    │
│   │    {"job_id": 11, "run_id": 101, "repo": "org-A/repo1", ...},       │    │
│   │    {"job_id": 12, "run_id": 102, "repo": "org-A/repo2", ...},       │    │
│   │    ...                                                               │    │
│   │  ]                                                                   │    │
│   │                                                                      │    │
│   │  org:B:pending = [                                                   │    │
│   │    {"job_id": 21, "run_id": 201, "repo": "org-B/repo1", ...},       │    │
│   │    ...                                                               │    │
│   │  ]                                                                   │    │
│   │                                                                      │    │
│   └─────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
│   Runner 완료 시 → 해당 Org 대기열에서 Job 꺼내서 Runner 생성                 │
│                                                                               │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 대기열 저장 정보

제한에 도달하면 전체 Job 정보를 Redis에 저장합니다:

```python
# Redis 대기열에 저장되는 정보 (JSON)
{
    "job_id": 12345,
    "run_id": 67890,
    "job_name": "build",
    "repo_full_name": "org-A/my-repo",
    "labels": ["code-linux"],
    "org_name": "org-A"
}
```

### 처리 순서

```
1. A-job-11 queued 이벤트 도착
   └── JIT Runner Manager: "A org 이미 10개 실행 중"
   └── Redis 대기열에 저장: org:A:pending.push(job-11 정보)

2. A-job-1 completed 이벤트 도착
   └── JIT Runner Manager: Redis 카운터 감소 (A: 10→9)
   └── JIT Runner Manager: K8s Pod 삭제
   └── JIT Runner Manager: Redis 대기열 확인
   └── 대기 Job 발견! → process_workflow_job_queued.delay(job-11 정보)

3. Celery Worker가 job-11 처리
   └── A org: 9/10 (여유 있음) → Runner 생성!
```

### 구성요소별 역할

| 구성요소 | 역할 | 대기열 관리 |
|---------|------|------------|
| FastAPI Webhook | 이벤트 수신 | ❌ |
| Celery Worker | Runner 생성/삭제, **대기열 처리** | ✅ |
| **Redis** | 상태 저장, **대기열 저장** | ✅ |
| GitHub | Workflow 실행 | ❌ (Webhook 재전송 안함) |

---

## 시나리오 예시

### 시나리오: 대규모 동시 요청

**조건:**
- A org: 35개 요청, limit: 10
- B org: 50개 요청, limit: 10
- C org: 72개 요청, limit: 10
- 전체 max_total: 100

**결과 분석:**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              요청 현황                                       │
│                                                                              │
│   Organization │ 요청 수 │ Org Limit │ 할당됨 │ Redis 대기열                │
│   ─────────────┼─────────┼───────────┼────────┼─────────────────            │
│   A org        │   35    │    10     │   10   │  25개 대기                  │
│   B org        │   50    │    10     │   10   │  40개 대기                  │
│   C org        │   72    │    10     │   10   │  62개 대기                  │
│   ─────────────┼─────────┼───────────┼────────┼─────────────────            │
│   합계         │  157    │     -     │   30   │ 127개 대기                  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

**핵심 포인트:**
- 전체 max_total이 100이지만, **Org별 limit(10)으로 인해 30개만 사용**
- 127개 요청은 **Redis 대기열**에서 순서 대기
- Runner 완료 시 해당 Org의 대기열에서 다음 Job 처리

### 시나리오: 커스텀 제한 적용

**조건:**
- A org: 35개 요청, **custom limit: 25**
- B org: 50개 요청, **custom limit: 50**
- C org: 72개 요청, limit: 10 (기본값)
- 전체 max_total: 100

**결과:**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│   Organization │ 요청 수 │ Org Limit │ 할당됨 │ 비고                        │
│   ─────────────┼─────────┼───────────┼────────┼─────────────────            │
│   A org        │   35    │    25     │   25   │  커스텀 limit               │
│   B org        │   50    │    50     │   50   │  커스텀 limit               │
│   C org        │   72    │    10     │   10   │  기본 limit                 │
│   ─────────────┼─────────┼───────────┼────────┼─────────────────            │
│   합계 (제한前)│  157    │     -     │   85   │                             │
│                                                                              │
│   BUT! 전체 max_total = 100                                                  │
│   실제 할당: A(25) + B(50) + C(10) = 85개 (또는 전체 100개까지)              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 시간에 따른 할당 변화

```
시간   이벤트              Org   상태                              결과
─────────────────────────────────────────────────────────────────────────────
T1    A-job-1 queued      A     A: 0/10, Total: 0/100            ✅ Runner 생성
T2    C-job-1 queued      C     C: 0/10, Total: 1/100            ✅ Runner 생성
T3    B-job-1 queued      B     B: 0/10, Total: 2/100            ✅ Runner 생성
...
T30   C-job-10 queued     C     C: 9/10, Total: 29/100           ✅ Runner 생성
T31   A-job-11 queued     A     A: 10/10 ❌ Org limit 도달        📥 Redis 대기열
T32   B-job-11 queued     B     B: 10/10 ❌ Org limit 도달        📥 Redis 대기열
...
T100  A-job-1 completed   A     A: 10→9, Total: 30→29            🔄 Pod 삭제
                                Redis 대기열 확인 → A-job-11 발견
T101  (내부) A-job-11     A     A: 9/10, Total: 29/100           ✅ Runner 생성
      태스크 호출됨
```

---

## FIFO 순서 보장

### Organization 내 순서

**Redis List를 사용하여 FIFO를 보장합니다:**

```python
# 대기열에 추가 (오른쪽 끝에 추가)
redis.rpush(f"org:{org_name}:pending", job_data)

# 대기열에서 꺼내기 (왼쪽 끝에서 꺼냄 = FIFO)
job_data = redis.lpop(f"org:{org_name}:pending")
```

```
A org 대기열 (FIFO):
  [A-job-11] → [A-job-12] → [A-job-13] → ...
       ↑                                    ↑
      먼저 꺼냄                          나중에 추가됨
```

### Organization 간 순서

`process_pending_queues` 태스크가 각 Org의 대기열을 순회하며 처리합니다:

```
process_pending_queues 실행 시:
  → A org 확인: running < limit? → Yes → A-job-11 처리
  → B org 확인: running < limit? → Yes → B-job-11 처리
  → C org 확인: running < limit? → No → 건너뜀
  → (5초 후 다시 실행)
```

각 Org는 독립적으로 대기열이 관리되므로, Org 간 공정성이 자연스럽게 보장됩니다.

---

## FAQ

### Q1: completed 이벤트를 처리하지 않으면 어떻게 카운터가 감소하나요?

**A:** `process_pending_queues` 태스크가 5초마다 K8s Pod 상태를 조회하여 카운터를 동기화합니다.

```python
# process_pending_queues 실행 시
def _sync_running_state(redis_client, k8s_client):
    # K8s에서 Running/Pending Pod만 조회
    running_pods = k8s_client.list_runner_pods()
    
    # 실제 Running Pod 수로 Redis 카운터 갱신
    # → 종료된 Pod는 자동으로 카운트에서 제외
    redis_client.set_total_running_sync(actual_count)
```

Ephemeral Runner는 Job 완료 후 자동 종료되므로, 다음 조회 시 카운터가 자동으로 감소합니다.

### Q2: Redis 대기열에 어떤 정보가 저장되나요?

**A:** Runner 생성에 필요한 모든 정보가 JSON 형태로 저장됩니다.

```json
{
    "job_id": 12345,
    "run_id": 67890,
    "job_name": "build",
    "repo_full_name": "org-A/my-repo",
    "labels": ["code-linux"],
    "org_name": "org-A"
}
```

### Q3: 전체 limit보다 Org limit 합계가 작으면?

**A:** Org별 limit에 의해 전체 limit을 다 사용하지 못할 수 있습니다.

```
예시:
  - max_total: 100
  - 10개 Org × 10 limit = 최대 100개 가능
  - 3개 Org × 10 limit = 최대 30개만 사용 (70개 유휴)
```

해결책: 활성 Org가 많으면 Org limit을 높이거나, max_total을 낮춥니다.

### Q4: Runner 생성에 실패하면?

**A:** Celery가 자동 재시도합니다.

```python
@celery_app.task(bind=True, max_retries=3, default_retry_delay=30)
def create_runner_for_job(self, ...):
    try:
        # JIT Token 발급
        jit_config = github_client.create_jit_runner_config(...)
    except Exception as e:
        raise self.retry(exc=e)  # 30초 후 재시도 (최대 3회)
```

### Q5: Redis와 실제 Pod 수가 불일치하면?

**A:** `process_pending_queues`가 5초마다 상태를 동기화합니다.

```python
# process_pending_queues 태스크 (5초마다 실행)
# 대기열 처리 전에 항상 K8s 상태 동기화 수행
def _sync_running_state(redis_client, k8s_client):
    running_pods = k8s_client.list_runner_pods()
    # Running/Pending Pod만 카운트하여 Redis 갱신
```

추가로 `sync_redis_state` 태스크가 5분마다 전체 동기화를 수행합니다.

### Q6: Redis 장애 시 대기열이 손실되면?

**A:** Redis persistence 설정을 권장합니다.

```yaml
# values.yaml
redis:
  master:
    persistence:
      enabled: true
      size: 1Gi
```

또한, GitHub의 Job 타임아웃(기본 6시간) 내에 Redis가 복구되면 수동으로 재처리할 수 있습니다.

### Q7: 대기 중인 Job이 GitHub에서 취소되면?

**A:** 현재 구현에서는 Redis 대기열에 남아있게 됩니다. 주기적인 정리가 필요합니다.

향후 개선 방안:
- `workflow_job.completed` (conclusion: cancelled) 이벤트 수신 시 대기열에서도 제거
- 주기적으로 GitHub API를 호출하여 취소된 Job 정리

---

## 관련 문서

- [README.md](../README.md) - 전체 시스템 개요
- [Enterprise Webhook 설정](enterprise-webhook-setup.md) - Webhook 구성 가이드
- [GitHub Actions - Self-hosted runners](https://docs.github.com/en/actions/hosting-your-own-runners)
