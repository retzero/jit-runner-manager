apiVersion: v1
kind: Service
metadata:
  name: {{ include "jit-runner-manager.fullname" . }}
  labels:
    {{- include "jit-runner-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jit-runner-manager.selectorLabels" . | nindent 4 }}
    component: webhook

